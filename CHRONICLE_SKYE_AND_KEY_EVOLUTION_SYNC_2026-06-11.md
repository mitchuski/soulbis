# Chronicle — /skye + key-evolution sync (2026-06-11)

Second sync from the star repo (the first: 2026-06-10, see
`CHRONICLE_AGENT_SYNC_PLAN_TO_STAR_2026-06-10.md`). Source of truth:
`C:\Users\mitch\star` — full build record there in
`CHRONICLE_THE_DESIGNED_NOW_BUILT_2026-06-11.md`.

## What changed in this directory

1. **New page `skye/index.html`** — 🌌 The Skye, the night of many keys:
   import any number of keys (JSON or sigil PNG), each rises as a star placed
   by its own κ; gold threads = lineage (`prior` chain), teal threads = common
   ground. Entirely client-side. Same-day additions, re-synced: **📸 sky shot**
   (the final export — a PNG carrying every risen key in a `citySky` tEXt
   chunk; drop it back and the whole night rises again) and **🤝 common ground
   (DH-PSI)** — blinded set intersection (RFC 3526 modp2048, verified safe
   prime), two-file exchange, learn the ∩ and only the ∩; scope chips cap what
   enters.
2. **`star/ lattice/ sigil/ guide/` re-synced byte-identical** with the star
   repo, carrying the day's features in:
   - `prior` κ-chain (evolved exports name their ancestor; idempotent re-export)
   - `figures` FIG-2.0 consumption (/star derives geometry from measured
     figures; /sigil petals scale by visibility, ⚡ petal floors by zkp)
   - ⚡ **charge pass** on /lattice (redacted export: trace+witness+κ only)
   - function portraits on /star (⚔️ neg chords · 🧙 bnot mirrors etoggles)
   - 🌌 /skye in the nav canon on every suite page; /guide has four rooms
3. **`index.html` (landing)** — nav gains `Skye` after `Sigil`; the Project
   node links `/skye →`; the Forge card gains `Gather the skye →`.
4. **Built section reconciled against github.com/mitchuski** (same day): added
   `star` + `myterms` + `soulbis` (Protocol) · `spellweb` + `BGIN AI` (Agents)
   · `mysword` + `mymage` (Tools) · `agentprivacy-skills` repo link (Personas)
   · `agentprivacy-cosmic` (Writing); agentprivacy-docs description bumped to
   the V6 register. Deliberately not listed: forks (bgin-homepage,
   cs-video-courses, plurality, characterfile_KYRA, mage-x-feed-filter is kept
   as the lone listed fork since it ships a tool), `swordsman`/`mage`
   (superseded by mysword/mymage — confirm before listing), `mage.city` (low
   signal beside cityofmages), `soulbae`/`agentkyracodex` (already represented
   by their live surfaces), `bgin-agents` (named inside the BGIN AI entry).

## Verification at sync time

- All five page copies byte-identical (cmp) against the star repo.
- Conformance vector holds: `sha256:0b4916ba…c527`, 303 canonical bytes.
- Routes 200 on the local review server (:6000): `/ /guide /star /lattice
  /sigil /skye`.

Deploy is the bearer's move, as ever.
